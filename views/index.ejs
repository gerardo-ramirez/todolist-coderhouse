<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://bootswatch.com/4/materia/bootstrap.min.css">
    <link rel="stylesheet" href="./../css/style.css">
    <title>Task CoderHouse</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="#"> Lista de Tareas</a>

    </nav>
    <div class="container">
        <div class="row">
            <div class="col-md-5 mt-3 mb-5">
                <div class="card">
                    <div class="card-header">
                        <h2>Agregar Tareas</h2>
                    </div>
                    <div class="card-body">
                        <form action="/add"  method="POST">
                        <div class="form-group">
                            <input name="title" placeholder="Debo hacer..." type="text" class="form-control">
                        </div>
                        <div class="form-group">
                            <textarea name="description" col="80" placeholder="Descripción"  class="form-control"></textarea>
                        </div>
                        <div class="form-group">
                            <input type="submit" value='Agregar' class="btn btn-info btn-block">
                        </div>

                        </form>
                    </div>

                </div>
            </div>
            <div class="col-md-7 col-sm-7 col-xs-5  mt-3 mb-5">
                <div class="card see">
                    <h2>Tareas:</h2>
                  <table class="table table-bordered table-hover">
                      <thead>
                          <tr>
                              <th>Numero</th>
                              <th>Titulo</th>
                              <th>Descripción</th>
                              <th>Acción</th>

                          </tr>
                      </thead>
                      <tbody>
                            <% for (let i=0; i<task.length; i++){ %>
                        
                            <tr>
                            <td><%= i + 1 %></td>
                            <td><%= task[i].title %></td>
                            <td><%= task[i].description %></td>  
                            <td>
                                <a href="/status/<%= task[i]._id%>" 
                                class="btn <%= task[i].status ? 'btn-success' : 'btn-secondary' %>">Status ok</a>
                                <a href="/update/<%= task[i]._id%>" class="btn btn-info">Actualizar</a>
                                <a href="/delete/<%= task[i]._id%>" class="btn btn-danger">Borrar</a>


                            </td>
                            </tr>

                         <% }   %>
                      </tbody>
                  </table>
                </div>

            </div>
            <div class="poster">
                <h4>Ahora podés anotarlas</h4>
                <p>No te olvides ningúna!!!</p>
            </div>
        </div>
    </div>
    
</body>
</html>